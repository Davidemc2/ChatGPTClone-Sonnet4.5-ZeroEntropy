{
  "name": "zero-entropy-chatgpt-clone",
  "version": "2.0.0",
  "description": "Zero Entropy ChatGPT Clone with enhanced RAG system for Vercel deployment",
  "main": "api/chat.py",
  "engines": {
    "node": "18.x",
    "python": "3.9.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "setup-db": "psql $DATABASE_URL -f database/schema.sql",
    "test": "python test_system.py"
  },
  "keywords": [
    "chatgpt",
    "ai",
    "rag",
    "zero-entropy",
    "vercel",
    "postgresql",
    "openai"
  ],
  "author": "Zero Entropy AI Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/zero-entropy-chat.git"
  },
  "dependencies": {
    "vercel": "^32.0.0"
  },
  "devDependencies": {
    "@vercel/python": "^4.0.0"
  }
}

